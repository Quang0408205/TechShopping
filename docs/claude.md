# Claude Code handoff

1. Read **[PENDING_WORK.md](PENDING_WORK.md)** first: the open work, in order, in Vietnamese.
2. Then read **[CLAUDE_CONTEXT.md](CLAUDE_CONTEXT.md)**: the full, current handoff (architecture, API, tests, decisions, git state).
3. Inspect the current codebase and check that it matches both files; report any mismatch before changing anything.

Continue from the first unfinished item in `PENDING_WORK.md` (also §10 "EXACT NEXT STEP" of `CLAUDE_CONTEXT.md`).
At the end of each session, update `CLAUDE_CONTEXT.md` and `PENDING_WORK.md` (not this file).
Browser test tools: `tools/e2e/` (see its README).
