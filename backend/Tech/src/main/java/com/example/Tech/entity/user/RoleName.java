package com.example.Tech.entity.user;

/**
 * Built-in roles. They are stored as plain names in roles.name and created at startup
 * by AuthDataInitializer when missing.
 */
public enum RoleName {
    CUSTOMER,
    STAFF,
    ADMIN
}
